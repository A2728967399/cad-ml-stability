# Primary-analysis sources with English directory paths

These files retain the original analysis logic. Only applicable directory-path strings were adapted to the English repository layout; the nested pipeline itself remains byte-identical to its original source. See the repository-root SOURCE_MANIFEST.json, PATH_PORTABILITY.md and SOURCES.md for original and adapted hashes, dependencies and safe-test instructions. Clinical input data and retained run artifacts are not distributed. Do not execute every script or assume that every --self-test option is data-free.

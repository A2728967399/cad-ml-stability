# Statistical utility dependency

This directory retains the byte-identical metric utility source used by the analysis. Only its containing directories were renamed; no source-code edit was needed. No source dataset, clinical result, fitted model or patient-level artifact is included. See the repository-root SOURCE_MANIFEST.json for its original and repository hashes and PATH_PORTABILITY.md for the directory mapping.
